# GitHub Actions Secrets Configuration Template
# This file documents all required and optional secrets
# Do NOT commit actual secrets - use GitHub Secrets UI instead

# ============================================
# CI Workflow Secrets
# ============================================
# No secrets required for CI workflow
# Set these optional secrets for faster builds:

# TURBO_TOKEN - Turbo cache remote token
# Get from: https://vercel.com/account/tokens
# TURBO_TOKEN=

# TURBO_TEAM - Turbo team name for cache
# TURBO_TEAM=


# ============================================
# Desktop Release Workflow Secrets
# ============================================

# --- Tauri Signing (REQUIRED for auto-updates) ---
# Generated with: pnpm tauri signer generate
# Set the password when generating the key

TAURI_SIGNING_PRIVATE_KEY=
# Base64 encoded private key from tauri signer generate

TAURI_SIGNING_PRIVATE_KEY_PASSWORD=
# Password used when generating the signing key


# --- macOS Code Signing (REQUIRED for macOS builds) ---
# Setup guide:
# 1. Export certificate from Keychain as .p12
# 2. Base64 encode: base64 -i certificate.p12
# 3. Generate app-specific password: https://appleid.apple.com/

APPLE_CERTIFICATE=
# Base64 encoded P12 certificate

APPLE_CERTIFICATE_PASSWORD=
# Password for the P12 certificate

APPLE_SIGNING_IDENTITY=
# Format: "Developer ID Application: Your Name (XXXXXXXXXX)"
# Find with: security find-identity -v -p codesigning

APPLE_ID=
# Apple ID email address

APPLE_TEAM_ID=
# Team ID (10-character code)
# Extract from signing identity above in parentheses

APPLE_PASSWORD=
# App-specific password (NOT your main Apple password!)
# Generate: https://appleid.apple.com/account/security


# --- Windows Code Signing (OPTIONAL) ---
# Only required if you want to sign Windows executables
# Omit these to build unsigned Windows installers

WINDOWS_SIGN_CERT=
# Base64 encoded PFX certificate

WINDOWS_SIGN_CERT_PASSWORD=
# Password for the PFX certificate


# ============================================
# Backend Deploy Workflow Secrets
# ============================================

# FLY_API_TOKEN (REQUIRED)
# Generate: flyctl tokens create
# Permissions: read+write for deployment
FLY_API_TOKEN=

# FLY_APP_NAME (REQUIRED)
# Fly.io application name
# Example: focus-app-prod
FLY_APP_NAME=


# ============================================
# Preview Deploy Workflow Secrets
# ============================================

# FLY_API_TOKEN (REQUIRED)
# Shared with main deployment
# (See above)

# FLY_ORG (OPTIONAL)
# Fly.io organization name
# Default: Uses account default org
FLY_ORG=


# ============================================
# Additional Optional Secrets
# ============================================

# GitHub Token (auto-provided, but can override)
# Usually not needed - GitHub provides GITHUB_TOKEN
# GITHUB_TOKEN=

# Slack Webhook (for notifications)
# Format: https://hooks.slack.com/services/YOUR/WEBHOOK/URL
# SLACK_WEBHOOK=

# Discord Webhook (for notifications)
# Format: https://discordapp.com/api/webhooks/YOUR/WEBHOOK
# DISCORD_WEBHOOK=


# ============================================
# Environment Variables (not secrets)
# ============================================
# These are set in workflow files, not as secrets

# Node version: 20
# pnpm version: 10.11.0
# Rust toolchain: stable
