{
  "name": "@focusflow/trailbase",
  "version": "0.1.0",
  "private": true,
  "description": "TrailBase configuration and migrations for FocusFlow",
  "scripts": {
    "up": "docker compose up -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "reset": "docker compose down -v && docker compose up -d"
  }
}
